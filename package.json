{
  "name": "gatsby-source-tune",
  "version": "1.0.0",
  "description": "Gatsby plugin to expose TUNE data as GraphQL nodes",
  "license": "MIT",
  "scripts": {
    "build": "babel src --out-dir . --ignore __tests__",
    "watch": "babel -w src --out-dir . --ignore __tests__",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin"
  ],
  "author": "Alan Shaw",
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "chokidar": "^1.7.0",
    "fs-extra": "^4.0.1",
    "lodash": "^4.17.4",
    "slash": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-2": "^6.24.1",
    "cross-env": "^5.0.5",
    "standard": "^10.0.3"
  }
}
